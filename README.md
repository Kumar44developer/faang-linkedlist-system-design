# faang-linkedlist-system-design
FAANG-style system design problems implemented in C++ using linked lists only — includes LRU cache, LFU cache, browser history, playlist manager, free-list memory allocator, and undo-redo system.
